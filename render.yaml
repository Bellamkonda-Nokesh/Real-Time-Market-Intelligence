services:
  - type: web
    name: marketnexus-ai
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: marketnexus-ai-db
          property: connectionString
      - key: NEWS_API_KEY
        sync: false   # You will paste this manually in Render dashboard

databases:
  - name: marketnexus-ai-db
    databaseName: marketnexus_ai_db
    user: marketnexus_user
    plan: free
