node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables - NEVER commit secrets
.env
.env.local
.env.production

# Replit config (not needed outside Replit)
.replit
replit.nix
.local